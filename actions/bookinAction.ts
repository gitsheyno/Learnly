"use server";

//TODO write in db and send for tutor and redirect to the dashboard and update  user dashboard
export const bookTutor = async (id : string ,formData: FormData) => {
  console.log("form", formData);
  console.log("id", id);
};
